name: agendaflutter
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.8
  sqflite: ^2.2.8+4
  path: ^1.8.1
  path_provider: ^2.0.11
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^8.0.0  # Adiciona o flutter_secure_storage para armazenamento seguro

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true